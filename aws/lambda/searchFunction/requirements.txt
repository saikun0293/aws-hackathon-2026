# AWS Lambda - Search Function Dependencies
# Install with: pip install -r requirements.txt -t .

# HTTP requests library
requests==2.31.0

# AWS SDK (usually pre-installed in Lambda, but included for local testing)
boto3>=1.34.0
botocore>=1.34.0
